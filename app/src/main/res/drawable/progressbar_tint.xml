<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <solid android:color="@color/white" /> <!-- Set the color of the background -->
        </shape>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <solid android:color="@color/red" /> <!-- Set the color of the progress -->
            </shape>
        </clip>
    </item>
</layer-list>